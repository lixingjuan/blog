/* ****************************************************************************************************
 * Object.create - 创建一个新对象，并将其原型指定为参数1
 ************************************************************************************************* */

var parent = {
  sayHello: function () {
    console.log("Hello");
  },
};

var child = Object.create(parent);
