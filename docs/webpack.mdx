---
sidebar_position: 10
---

import CodeBlock from "@theme/CodeBlock";
import Button from "@site/src/components/Button";
import Space from "@site/src/components/Space";

import fn from "./code/***";
import Code from "!!raw-loader!./code/***";

## loader 和 plugin

loader, 本质上是导出为函数的 JavaScript 模块，loader runner 会调用此函数，然后将上一个 loader 产生的结果或者资源文件传入。
loader 的执行是有顺序的

## Code

<CodeBlock language="js">{Code}</CodeBlock>
